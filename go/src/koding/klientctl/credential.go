package main

import (
	"github.com/codegangsta/cli"
	"github.com/koding/logging"
)

func CredentialImport(c *cli.Context, log logging.Logger, _ string) int {
	return 0
}

func CredentialList(c *cli.Context, log logging.Logger, _ string) int {
	return 0
}

func CredentialAdd(c *cli.Context, log logging.Logger, _ string) int {
	return 0
}
